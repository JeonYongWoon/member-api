
# application.yml
spring:
  application:
    name: member-api
  profiles:
    active: prod
  config:
    import:
      aws-parameterstore:

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health, info

cloud:
  aws:
    s3:
      bucket: member-api-profile-oioipo22
    region:
      static: ap-northeast-2
